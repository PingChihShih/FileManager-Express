extends layout

block content
  h1= title
  p Welcome to #{title}
  form(action='/files/upload' enctype='multipart/form-data' method='post')
    .form-group
      input.form-control-file(type='file' name='uploaded_file')
      input.btn.btn-default(type='submit' value='Submit')

